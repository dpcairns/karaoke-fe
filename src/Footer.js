import React, { Component } from 'react'

export default class Footer extends Component {
    render() {
        // this dead code should be deleted to avoid cluttering up the project
        return (
            <div className='footer'>
                
            </div>
        )
    }
}
